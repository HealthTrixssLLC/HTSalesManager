# Health Trixss CRM - Environment Variables Configuration
# Copy this file to .env and update with your actual values

# ========================================
# Application Configuration
# ========================================

# Environment: development | production
NODE_ENV=development

# Server port (default: 5000)
PORT=5000

# ========================================
# Database Configuration (PostgreSQL)
# ========================================

# Full PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://healthtrixss:your_password@localhost:5432/healthtrixss_crm

# Individual PostgreSQL connection parameters (alternative to DATABASE_URL)
# These are automatically set by Replit's database integration
PGHOST=localhost
PGPORT=5432
PGUSER=healthtrixss
PGPASSWORD=your_password
PGDATABASE=healthtrixss_crm

# ========================================
# Security & Authentication
# ========================================

# JWT secret for authentication tokens
# IMPORTANT: Generate a strong random string for production
# Example: openssl rand -base64 32
SESSION_SECRET=change_this_in_production_use_strong_random_string

# Backup encryption key (AES-256-GCM)
# IMPORTANT: Generate a 32-byte key for production
# Example: openssl rand -base64 32
BACKUP_ENCRYPTION_KEY=change_this_in_production_use_32_byte_key

# ========================================
# Docker Compose Variables (for self-hosted deployment)
# ========================================

# PostgreSQL password for Docker deployment
DB_PASSWORD=your_secure_database_password

# ========================================
# Production Deployment Notes
# ========================================

# 1. SESSION_SECRET: Generate with `openssl rand -base64 32`
# 2. BACKUP_ENCRYPTION_KEY: Generate with `openssl rand -base64 32`
# 3. DB_PASSWORD: Use a strong password (minimum 16 characters)
# 4. DATABASE_URL: Update with production database credentials
# 5. Never commit .env file to version control
# 6. For Replit deployment, set these in Secrets (not .env file)

# ========================================
# Development Defaults (DO NOT USE IN PRODUCTION)
# ========================================

# The following values are automatically set for development if not provided:
# - BACKUP_ENCRYPTION_KEY: "dev-backup-key-2025-healthtrixss-crm-secure"
# - SESSION_SECRET: "health-trixss-crm-secret-key"
# - PORT: 5000
